
/**
 * This seemingly useless file actually gets populated by NodObjC throughout
 * use. When symbols are imported from Objective-C, they get placed here.
 *
 * This allows the same object to be re-used for "module-mode" NodObjC as well
 * as "global-mode" NodObjC.
 */

module.exports = {}
